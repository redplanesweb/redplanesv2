import { Button } from '@material-ui/core'
import ButtonWrapper from '@components/MuiButtonWrapper'

const Pages = () => {

    return <ButtonWrapper variant="contained" color="primary" className="hover-grow">Write to Fauna</ButtonWrapper>
}

export default Pages